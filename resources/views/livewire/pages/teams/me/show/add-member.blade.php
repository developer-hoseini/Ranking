<div class="add_member">
    <form
        {{-- method="post" --}}
        {{-- action="{{ route('add_member') }}" --}}
    >

        {{-- <add-member
            random-search-route="{{ route('team_random_users') }}"
            search-route="{{ route('team_search_user') }}"
            select-route="{{ route('team_select_user') }}"
            profile-link="{{ route('profile', ['username' => '']) }}"
            lbl-username="{{ __('words.Username') }}"
            lbl-suggested="{{ __('words.suggested:') }}"
            lbl-score="{{ __('words.Score: ') }}"
            lbl-rank="{{ __('words.Rank: ') }}"
            lbl-btn="{{ __('words.invite to team') }}"
            lbl-required="{{ __('words.Enter Username') }}"
            game-id="{{ $team->game_id }}"
        ></add-member> --}}

    </form>
</div>
